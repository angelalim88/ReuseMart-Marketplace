import React from 'react';
import { decodeToken } from "../../utils/jwtUtils";

// console.log(decodeToken(localStorage.getItem("authToken")));

const PembeliPage = () => {
  return <div>Pembeli Page (Kosong)</div>;
};

export default PembeliPage;